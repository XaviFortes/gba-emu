[package]
name = "gba-emu"
version = "0.1.0"
edition = "2024"

[dependencies]
minifb = "0.27"
rodio = { version = "0.19", default-features = true, optional = true }

[features]
default = []
audio = ["dep:rodio"]
